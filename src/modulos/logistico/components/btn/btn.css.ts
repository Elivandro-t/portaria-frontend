import styled from "styled-components";

export default {
    btnAdd: styled.button`
      padding: 12px 24px;
      border-radius: 6px;
      border: none;
      background: #16a34a;
      color: #fff;
      font-size: 15px;
      cursor: pointer;
      &:hover{
      opacity: 0.9;
    }
    `
}