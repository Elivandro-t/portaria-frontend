import styled from "styled-components";

export default {
    container:styled.div`
        display: flex;
        gap: 10px;
        align-items: center;
    `,
    Or:styled.span`
    text-align: center;
        color: var(--cor-header-principal);
    `
}